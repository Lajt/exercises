function sym(args) {
  //arguments
  const arr = []
  for(let i = 0 ; i < arguments.length; i ++)
    arr.push(arguments[i])

  arr.map((ar, idx) => {
    
  })
  
  console.log(arr)
}

console.log(sym([1, 2, 3], [5, 2, 1, 4]));
